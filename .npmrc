# @cloudflare/next-on-pages and wrangler declare conflicting optional peer
# ranges for @cloudflare/workers-types (v4 vs v5). Both are types-only and
# optional; legacy-peer-deps lets the Cloudflare + Next toolchain install
# cleanly and keeps `npm ci` consistent between local and CI.
legacy-peer-deps=true
