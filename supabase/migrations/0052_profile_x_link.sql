-- =============================================================================
-- プルミエ！ 0052 — プロフィールに X（旧Twitter）リンク
--   レイヤー/カメラマンが自分の X アカウントをプロフィールに載せられるように、
--   ハンドル（@なしのユーザー名）を1つ保存する列を追加する。表示URLは
--   アプリ側で https://x.com/<handle> を組み立てるため、URLではなくハンドル
--   のみを保持する（表記ゆれを避け、正規化はアプリ側の保存時に行う）。
--   本人のみ更新可（既存の profiles_update RLS: id = auth.uid() がそのまま効く）。
--   冪等（add column if not exists、再実行安全）。
-- =============================================================================

alter table profiles add column if not exists x_handle text;
