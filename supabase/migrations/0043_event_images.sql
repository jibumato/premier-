-- =============================================================================
-- プルミエ！ 0043 — イベントのサムネイル画像
--   events に画像URL列を追加する。大型イベントの公式ロゴ・キービジュアルは
--   プレス審査制で第三者が自由に使えないため、**主催から許諾を得た画像**か
--   **運営が自ら撮影した会場写真**だけを入れる運用（詳細はチェックリスト）。
--   未設定のイベントは、イベント名から生成した権利リスクゼロのデザインで
--   表示されるので、空のままでも見栄えは保たれる。
-- =============================================================================

alter table events add column if not exists image_url text;
