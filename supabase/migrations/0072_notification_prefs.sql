-- =============================================================================
-- プルミエ！ 0072 — 通知の種別ごとのON/OFF設定
--   通知が増えた（応募・メッセージ・イベントのリマインド・レビュー催促・フォロー/
--   いいね等）ので、ユーザーが種別ごとにミュートできるようにする。うるさくて
--   離脱・アンインストールされるのを防ぐ、信頼の土台。
--
--   設計: オプトアウト方式。キーが無ければ「オン（届く）」。false のときだけミュート。
--   例: {"event": false} = イベントのリマインド・レビュー催促を止める。
--   併せの応募・メッセージ等の「重要な通知」は常に届く（UIでトグルを出さない）。
--   本人（id = auth.uid()）だけが更新できる（0001 の profiles_update ポリシー）。
-- =============================================================================

alter table profiles
  add column notification_prefs jsonb not null default '{}'::jsonb;
