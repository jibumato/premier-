-- =============================================================================
-- プルミエ！ 0059 — 通知種別に 'post'（新しい写真の投稿おしらせ）を追加
--   併せ仲間が写真を投稿したときのおしらせ（0060 で使用）に使う種別。
--   ※ enum への値追加は「追加したトランザクションのコミット後」でないと使えない
--     ため、テーブル/トリガー（0060）とは別ファイルに分けている。
--   冪等（if not exists）。
-- =============================================================================

alter type notification_type add value if not exists 'post';
