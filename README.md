# プルミエ！ — コスプレイヤー交流マッチング (プロトタイプ実装)

コスプレイヤー同士（カメラマンも参加可）が好きな作品でつながり、併せ撮影の
企画・募集・応募ができる交流プラットフォーム「プルミエ！」のモバイル画面実装です。

`design_handoff_premier_prototype`（Anthropic 社内デザインツールの `.dc.html`
リファレンス）を **見た目・レイアウト・状態遷移の仕様書** として参照し、本番用の
コードとして新規に実装し直したものです。デザインファイルはそのまま流用していません。

## 技術スタック

- **Next.js 15**（App Router）+ **React 19** + **TypeScript**
- モバイルWeb（390px 幅、デザインが対応する唯一のブレークポイント）
- Web フォントは Google Fonts の `Zen Kaku Gothic New` / `Zen Maru Gothic` を
  ランタイム `<link>` で読み込み（ビルド時の外部依存なし）
- アイコンはすべてインライン SVG（Feather 風ライン、stroke-width 1.6–2）

> 未確定だったフロントエンド技術スタックは、ハンドオフ README の推奨
> （「未確定ならモバイルWeb兼用の React（Next.js等）」）に従い Next.js を採用。
> 状態機械・データ・トークンは技術非依存に切り出してあり、React Native /
> Flutter への移植も容易です。

## 実装済み画面

### A. ハンドオフのプロトタイプに準拠（8 画面）

| screen key | 画面 | 概要 |
| --- | --- | --- |
| `home` | ホーム | 検索入口・機能ショートカット・人気作品チップ・併せ募集フィード・投稿グリッド |
| `search` | さがす | 地域フィルタ（チップ）／3 タブ／結果リスト／空状態カード |
| `detail` | 併せ募集 詳細＋応募 | ヒーロー・主催者カード・基本情報グリッド・募集キャラ・応募ボタン・通報導線 |
| `applied` | 応募完了 | チェックマーク確認画面＋メッセージ導線 |
| `create` | 併せ募集 作成 | プログレス・参考画像・入力欄・世界観タグ・トグル 2 種 |
| `created` | 公開完了 | 公開確認画面 |
| `notify` | おしらせ | 通知リスト（未読ハイライト） |
| `profile` | マイページ | カバー／グラデーションリングアバター／称号／スタッツ／アクション／応援リンク／応援ギフト／ギャラリー |

### B. 新規デザイン＋実装（旧「Not Yet Designed」機能）

参照デザインが無いため、上記のデザインシステム（トークン・共通コンポーネント）と
運営方針（Constraints）に沿って新規に設計・実装しました。

| screen key | 画面 | 概要 |
| --- | --- | --- |
| `messages` | メッセージ一覧 | 会話リスト（未読バッジ）。ホーム上部アイコンから |
| `chat` | 会話スレッド | 吹き出しチャット・入力送信・レビュー導線。応募完了/プロフから |
| `reviewWrite` | レビュー投稿 | 星評価＋良かった点タグ＋コメント、投稿で確認状態 |
| `market` | フリマ一覧 | 衣装・小道具の 2 列グリッド（SOLD 表示）。ホームショートカット |
| `marketDetail` | 商品詳細 | 画像・価格・出品者・スペック・出品者メッセージ・通報 |
| `events` | イベントカレンダー | イベント一覧（日付チップ・参加予定数） |
| `eventDetail` | イベント詳細 | 概要・基本情報・参加表明フロー（確認状態） |
| `qa` | 知恵袋一覧 | 質問カード（解決済/タグ/回答数） |
| `qaDetail` | 質問詳細 | 質問本文・回答リスト（ベストアンサー）・回答投稿 |
| `report` | 通報・ブロック | 理由選択＋詳細＋ブロック、送信で自動処理メッセージ |
| `settings` | 設定 | 通知/プライバシー/応援リンク表示切替/ブロック/法人導線/ログアウト |
| `corporate` | 法人向け掲載面 | スタジオ/イベント/メーカー広告の掲載案内ランディング |

**主な導線**: ホーム上部＝メッセージ／おしらせ、ホームのショートカット＝フリマ・イベント・知恵袋、
応募完了＝主催者メッセージ、マイページ＝設定（歯車）・アクション・通報、詳細/商品＝通報、
チャット＝レビュー、設定＝法人向け。

## 状態遷移・振る舞い

`clickable_prototype.dc.html` の DCLogic 状態機械を移植しています。

- `screen` — 現在画面、`tab` — 下部ナビのアクティブタブ、`history` — 戻る用スタック、
  `region` — 検索の地域フィルタ（初期値「すべて」）。
- 下部タブナビ（ホーム / さがす / ＋作成 / おしらせ / マイページ）。中央 ＋ は
  常時ラベンダーグラデーションの FAB。
- 地域チップのタップで結果リストと件数表示がクライアント側で再計算され、
  0 件時は空状態カードを表示。
- 作成フォームのトグル（女性限定 / 初心者歓迎）はインタラクティブ。

## 運営方針の反映（Constraints）

ワンオペ運営前提のため、ハンドオフの制約を UI に反映しています。

- 応援ギフト（投げ銭）UI は **将来機能のプレースホルダ**（サイト内決済は未実装）。
- 実装対象の収益化導線は **外部の応援リンク（Fantia / pixivFANBOX / Skeb）のみ**。
- 応援リンクは **年齢確認済み（18歳以上）ユーザーにのみ表示** するゾーニングを実装。
  「サイト内にアダルトコンテンツはありません」を明記。

## ディレクトリ構成

```
src/
  app/
    layout.tsx        # フォント読み込み・メタデータ
    globals.css       # リセット＋キャンバス背景
    page.tsx          # AppShell を描画
  lib/
    tokens.ts         # デザイントークン（色・角丸・影・余白）
    types.ts          # 画面キー・データ型
    data.ts           # モックデータ（本番は API 由来）
  components/
    AppRouter.tsx     # クライアント側ルーター（screen/tab/history/region）
    AppShell.tsx      # 画面スイッチャ
    PhoneFrame.tsx    # 390px フレーム＋ステータスバー＋下部ナビ
    StatusBar.tsx
    BottomNav.tsx
    ImageSlot.tsx     # 画像プレースホルダ（本番は Image + オブジェクトストレージ）
    icons.tsx         # インライン SVG アイコン
    ui.tsx            # 共通 UI（AppBar・見出し・プライマリボタン・Toggle）
    screens/          # 全 20 画面
```

## 開発

```bash
npm install
npm run dev     # http://localhost:3000
npm run build   # 本番ビルド
npm run lint
```

## 残りの未実装

ハンドオフの「Not Yet Designed」機能はセクション B として実装済みです。以下は
デザイン集（`screens_all_options`）に案があるものの本実装では未着手です。

- オンボーディング 3 ステップ（8a ロール選択 / 8b 好きな作品フォロー / 8c 本人確認）
- カメラマンプロフィール（7a、レイヤープロフィールとほぼ同骨格）

また、新規実装した機能は UI とクライアント側の状態のみで、以下は本番実装が別途必要です。

- 実画像のアップロード・保存・配信基盤（オブジェクトストレージ＋CDN）
- 本人確認の外部 eKYC 連携（年齢確認・確認済みバッジ）
- メッセージ/通報/フリマ取引/イベント参加などのサーバー永続化・リアルタイム同期
- 通報の自動処理パイプライン（即時非表示＋確認キュー）の実体
